*** Settings ***
Resource          ../pages/MainPage.txt
Resource          ../pages/dataset-pages/DatasetDetailsPage.txt
Resource          ../pages/directory-pages/FileDirectoryPage.txt
Resource          ../pages/dataset-pages/file-dataset-pages/SelectFileDatasourcePage.txt
Resource          ../pages/virtual-field-pages/VirtualFieldPage.txt

*** Keywords ***
Open Virtual Field Page
    [Arguments]    ${dataset_index}
    Select Virtual Field Menu Item    ${dataset_index}

Open Virtual Metric Page
    [Arguments]    ${dataset_index}
    Select Virtul Metric Menu Item    ${dataset_index}

Create Text Virtual Field
    [Arguments]    ${field_name}    ${vf_function}    ${original_field_type}
    #选择文本类型
    Select Text Type for Virtual Field
    #设置虚拟字段表达式
    Set Expression for Text Virtual Field    ${field_name}    ${vf_function}    ${original_field_type}
    #设置虚拟字段名称
    Set Name for Virtual Field    ${field_name}    ${vf_function}
    Click Create Virtual Field Button

Create Numeric Virtual Field
    [Arguments]    ${field_name}    ${vf_function}    ${original_field_type}
    #选择数值类型
    Select Numeric Type for Virtual Field
    #设置虚拟字段表达式
    Set Expression for Numeric Virtual Field    ${field_name}    ${vf_function}    ${original_field_type}
    #设置虚拟字段名称
    Set Name for Virtual Field    ${field_name}    ${vf_function}
    Click Create Virtual Field Button

Create Datetime Virtual Field by Input Expression
    [Arguments]    ${vf_expression}    ${vf_name}
    #选择日期类型
    Select Datetime Type for Virtual Field
    #输入虚拟字段表达式
    Input Expression for Virtual Field    ${vf_expression}
    #设置虚拟字段名称
    Input Name for Virtual Field    ${vf_name}
    Click Create Virtual Field Button

Create Text Virtual Field by Input Expression
    [Arguments]    ${vf_expression}    ${vf_name}
    #选择文本类型
    Select Text Type for Virtual Field
    #输入虚拟字段表达式
    Input Expression for Virtual Field    ${vf_expression}
    #设置虚拟字段名称
    Input Name for Virtual Field    ${vf_name}
    Click Create Virtual Field Button

Create Numeric Virtual Field by Input Expression
    [Arguments]    ${vf_expression}    ${vf_name}
    #选择数值类型
    Select Numeric Type for Virtual Field
    #输入虚拟字段表达式
    Input Expression for Virtual Field    ${vf_expression}
    #输入虚拟字段名称
    Input Name for Virtual Field    ${vf_name}
    Click Create Virtual Field Button

Create Virtual Metric
    [Arguments]    ${vm_expression}    ${vm_name}
    #输入聚合函数表达式
    Input Expression for Virtual Metric    ${vm_expression}
    #输入聚合函数名称
    Input Name for Virtual Field    ${vm_name}
    Click Create Virtual Field Button
