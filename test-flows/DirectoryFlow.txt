*** Settings ***
Resource          ../pages/MainPage.txt
Resource          ../pages/directory-pages/FileDirectoryPage.txt

*** Keywords ***
Create and Select a New Datasource Directory
    [Arguments]    ${dict_name}
    Select Datasource Tab in Navigation Bar
    Create a New Directory    MyDataSource
    Select the Third Directory

Create New Directory
    [Arguments]    ${directory_name}
    Create a New Directory by Name    ${directory_name}
